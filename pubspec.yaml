name: geoflutterfire
description: A new Flutter plugin.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.8.2+3

flutter:
  plugin:
    androidPackage: com.noeatsleepdev.geoflutterfire
    pluginClass: GeoflutterfirePlugin
